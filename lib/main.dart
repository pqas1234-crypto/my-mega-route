import 'package:flutter/material.dart';

void main() => runApp(MaterialApp(
  theme: ThemeData.dark(),
  home: Scaffold(
    appBar: AppBar(title: Text('MegaRoute 中部戰區導航')),
    body: Center(child: Text('全自動雲端編譯測試中...', style: TextStyle(fontSize: 20))),
  ),
));
